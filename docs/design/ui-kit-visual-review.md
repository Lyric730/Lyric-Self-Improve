# 云瀚 UI Kit 视觉对照验收记录

状态：UI-04 进行中
更新时间：2026-05-27

## 1. 当前机器验收

已完成：

- 微信开发者工具 CLI preview 可打开。
- 当前包体：`584.5 KB`。
- JS 语法检查通过。
- JSON 解析检查通过。
- 32 个 PNG 美术资产边缘检测通过。
- 自动化协议可以连接、跳转页面、读取 `pages/player/player` 数据。

未完成：

- 尚未拿到微信模拟器截图，因此不能把 UI-04 归档为完成。
- 已确认 DevTools `auto` 端口可用，`Tool.getInfo`、`App.getCurrentPage`、`App.getPageStack` 有返回。
- `App.captureScreenshot` 在当前微信开发者工具版本中不返回，`Tool.captureScreenshot` 和 `Page.captureScreenshot` 返回 `unimplemented`。
- 已尝试 Windows `PrintWindow` / `CopyFromScreen`，只能得到灰屏或黑屏，判断为 DevTools/NW GPU 合成层无法被普通窗口截图捕获。

## 2. 需要截图确认的范围

请保留以下三段截图，后续用于逐组件继续扣细节：

| 截图 | 范围 | 用途 |
| --- | --- | --- |
| 截图 A | 页面顶部到按钮状态矩阵 | 检查顶部阶段轨道、按钮分组、按钮质感 |
| 截图 B | 玩法卡片分层区域 | 检查抢 5 / 抢 7 / 抢 10 卡片比例、切角、拥挤度 |
| 截图 C | 美术资产组件区域 | 检查段位、星级、奖励、结算、胜负确认资产比例 |

## 3. 当前已知视觉差异

这些是基于设计稿、代码结构和资产预览得到的已知问题，不等同于最终视觉验收结论。

| 组件 | 当前状态 | 需要继续扣的点 |
| --- | --- | --- |
| `YhButton` | 已有主/次/危险/成功/图标按钮状态 | 主按钮的金属边、压暗状态、斜切厚度还需要更接近 07 号设计稿 |
| `ModeCard` | 已分出当前推荐、高收益、预留 | 四角内收结构还不够像设计稿，选中态的橙色边框和卡片厚度需要继续扣 |
| `RankBadge` | 已接入段位 PNG 和积分进度 | 徽章与文字比例需要看模拟器截图确认，进度条还偏普通 |
| `StarTrack` | 已展示加星、保护、扣星状态 | 缺少加星/扣星动画，星星间距需要实机确认 |
| `RewardCrate` | 已接入普通宝箱和冲刺宝箱 | 随机奖励应该更突出，当前文案和宝箱层级可能还不够强 |
| `SettlementBadge` | 已展示段位提升、加分、扣分、奖励 | 结算卡片比例仍偏组件库展示，后续应接近结算仪式感 |
| `VictoryBanner` | 已接入胜利横幅资产 | 胜利横幅在小程序区域内的尺度需要截图确认 |
| `AcceptStamp` | 已接入服了确认章资产 | 需要确认红章是否过小、是否有足够确认感 |
| `pages/ui-kit` | 已改成验收台结构 | 当前是可验收工具，不是最终球友端页面；后续仍要逐组件视觉精修 |

## 4. UI-04 完成标准

UI-04 只有在以下条件满足后才能归档：

1. 微信开发者工具模拟器截图已保存到 `docs/design/`。
2. 每个组件至少记录一条“通过 / 待修 / 不适用”的结论。
3. 待修问题拆成后续任务，不混在 UI-04 里。
4. `docs/design/ui-kit-task-tracker.md` 更新归档。
5. `docs/dev-log.md` 同步记录。

## 5. 当前可继续推进的部分

虽然 UI-04 缺真实截图，代码层面已经可以继续组装页面：

- `pages/player/player` 已作为球友端首屏接入。
- `pages/ui-kit/ui-kit` 保留为组件验收台。
- 后续 UI-06 必须基于微信开发者工具或用户提供的真实截图继续扣视觉细节，不能只看代码判断“像不像”。

更新：

- `pages/player/player` 已被判定为错误长页方向，不能作为业务入口。
- 当前真实首屏已改为 `pages/challenge-home/challenge-home`。
- 球友端流程已按 `docs/design/player-flow-page-spec.md` 拆分为多个独立页面。
- UI-06 后续应逐页对照截图，不再对一个长页面做视觉精修。
